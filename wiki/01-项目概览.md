# 项目概览

## 一、产品定位

GeniusFlow 是一个**智能业务流程管理平台**，核心理念是将 AI 能力嵌入流程的每个执行节点，实现：

- **质量自动巡检**：AI 督导员在节点提交后自动对比验收标准，输出质量报告
- **智能审批辅助**：AI 助理为管理层生成结构化的审批建议（通过/拒绝 + 置信度 + 风险分析）
- **知识库增强**：节点巡检时可关联企业知识库，引用规范文档作为参考
- **可视化流程设计**：基于 React Flow 的拖拽式画布，支持流程模板市场

---

## 二、功能模块

### 2.1 流程设计

| 功能 | 说明 |
|------|------|
| 流程定义管理 | 创建、编辑、发布、归档流程定义 |
| 可视化编辑器 | 拖拽节点、连线，配置每个节点的输入/输出规范和 AI 参数 |
| 模板市场 | 预置流程模板，支持一键复制并进入编辑 |
| 版本控制 | 流程名称+版本唯一约束，发布后不可修改（需新版本） |

### 2.2 流程执行

| 功能 | 说明 |
|------|------|
| 流程实例 | 基于已发布流程创建执行实例，支持计划时间设定 |
| 节点执行 | 执行人填写节点输出数据，保存草稿或提交 |
| AI 巡检 | 提交后异步触发 AI Inspector，生成质量报告 |
| 进度看板 | Gantt 图展示各节点计划/实际时间轴 |

### 2.3 审批工作流

| 功能 | 说明 |
|------|------|
| 审批任务 | 节点提交后自动创建审批任务，分配给责任人 |
| AI 辅助审批 | AI Assistant 生成审批建议，辅助管理者决策 |
| 审批记录 | 完整审批历史，支持拒绝并附原因 |

### 2.4 知识库

| 功能 | 说明 |
|------|------|
| 知识库管理 | 创建分类知识库（标准/规程/案例等） |
| 文档上传 | 支持 TXT、PDF、DOCX 等格式，异步解析+向量化 |
| 语义检索 | 向量相似度搜索，在 AI 巡检时自动关联相关片段 |

### 2.5 AI 配置（管理员）

| 功能 | 说明 |
|------|------|
| AI 提供商 | 管理 OpenAI / Anthropic / Ollama / Azure 等提供商的连接信息 |
| 模型管理 | 配置对话模型、嵌入模型，设定默认模型 |
| Fallback 链 | 为每种模型类型配置有序备用链，主模型失败时自动切换 |
| Agent 技能 | 定义 Agent 可调用的工具/检索/代码执行技能 |
| Agent 角色 | 配置 Agent 角色的系统提示词、绑定模型和技能 |

### 2.6 系统管理

| 功能 | 说明 |
|------|------|
| 用户管理 | 用户 CRUD，支持激活/停用 |
| 角色权限 | 角色定义与权限分配（admin / designer / manager / employee） |
| 审计日志 | 记录所有关键操作，含操作人、IP、时间 |
| 实时通知 | WebSocket 推送 + 站内消息，支持逾期自动扫描提醒 |

---

## 三、技术栈

### 后端 `api/`

| 技术 | 版本 | 用途 |
|------|------|------|
| NestJS | 10.x | 主框架，模块化 IoC |
| Prisma | 5.x | ORM，类型安全数据库操作 |
| PostgreSQL + pgvector | 16 | 关系数据存储 + 向量相似度搜索 |
| Redis + Bull | 7 / 4.x | 消息队列，异步 AI 任务 |
| Socket.IO | 4.x | WebSocket 实时通知 |
| JWT + Passport | — | 认证鉴权 |
| class-validator | 0.14 | 请求参数校验 |
| @nestjs/schedule | 6.x | 定时任务（逾期扫描） |

### 前端 `web/`

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18 | UI 框架 |
| Vite | 5 | 构建工具 |
| TypeScript | 5 | 类型安全 |
| Ant Design | 5 | UI 组件库 |
| @xyflow/react (React Flow) | 12 | 流程编辑器画布 |
| Zustand | 4 | 轻量状态管理 |
| TanStack Query | 5 | 服务端状态管理（部分接口） |
| Axios | 1.6 | HTTP 客户端 |
| Socket.IO Client | 4 | WebSocket 客户端 |
| React Router | 6 | 路由管理 |

### AI 服务 `ai-service/`

| 技术 | 版本 | 用途 |
|------|------|------|
| FastAPI | — | Python Web 框架 |
| OpenAI SDK | — | 调用 LLM 和嵌入模型 |
| pydantic-settings | — | 配置管理 |
| asyncpg / psycopg | — | 向量检索查询 |

---

## 四、整体架构

```
┌────────────────────────────────────────────────────────────┐
│                         浏览器客户端                         │
│               React 18 + Ant Design 5 + React Flow          │
└────────────────────┬───────────────────────────────────────┘
                     │ HTTP/WebSocket (port 80)
                     ▼
┌────────────────────────────────────────────────────────────┐
│                    Nginx 反向代理                           │
│   /        → web:3000                                      │
│   /api/    → api:4000                                      │
│   /ws/     → api:4000 (WebSocket)                          │
│   /ai/     → ai-service:5000                               │
└─────┬───────────────┬──────────────────────────────────────┘
      │               │
      ▼               ▼
┌──────────┐   ┌─────────────────┐
│  前端    │   │  NestJS API     │
│  Vite    │   │  port 4000      │
│  port    │   │  /api/v1/*      │
│  3000    │   └────┬───────┬────┘
└──────────┘        │       │
               ┌────┘       └────────┐
               ▼                     ▼
    ┌─────────────────┐   ┌────────────────┐
    │   PostgreSQL    │   │     Redis      │
    │   + pgvector    │   │   Bull Queue   │
    │   port 5432     │   │   port 6379    │
    └─────────────────┘   └───────┬────────┘
                                  │ 消费队列
                                  ▼
                       ┌────────────────────┐
                       │   FastAPI AI 服务  │
                       │   port 5000        │
                       │   /ai/inspect      │
                       │   /ai/assist       │
                       │   /ai/search       │
                       │   /ai/embed        │
                       └────────────────────┘
```

---

## 五、项目目录结构

```
geniusflow/
├── api/                    # NestJS 后端
│   ├── src/
│   │   ├── ai-reports/     # AI 报告读取接口
│   │   ├── ai-settings/    # AI 提供商/模型/技能/角色/Fallback 链管理
│   │   ├── audit-log/      # 审计日志
│   │   ├── auth/           # JWT 认证、角色守卫
│   │   ├── common/         # 全局拦截器、异常过滤器
│   │   ├── health/         # 健康检查
│   │   ├── knowledge-bases/ # 知识库 + 文档上传 + 向量检索
│   │   ├── node-definitions/ # 流程节点定义 CRUD
│   │   ├── node-instances/  # 节点实例执行（保存/提交/进度）
│   │   ├── notifications/   # 通知管理 + 逾期扫描
│   │   ├── prisma/          # Prisma 模块封装
│   │   ├── process-definitions/ # 流程定义 CRUD + 发布
│   │   ├── process-instances/   # 流程实例创建/终止/Gantt
│   │   ├── queue/           # Bull 队列服务封装
│   │   ├── roles/           # 角色管理
│   │   ├── scheduler/       # 定时任务（逾期扫描）
│   │   ├── tasks/           # 任务（审批任务）管理
│   │   ├── templates/       # 模板市场（克隆等）
│   │   ├── users/           # 用户管理
│   │   └── ws/              # WebSocket 网关
│   └── prisma/
│       ├── schema.prisma    # 数据模型
│       ├── migrations/      # 迁移历史
│       └── seed.ts          # 初始数据
├── web/                    # React 前端
│   └── src/
│       ├── api/             # 各模块 API 客户端
│       ├── components/      # 公共组件（布局、流程编辑器等）
│       ├── pages/           # 页面组件
│       ├── stores/          # Zustand 状态
│       ├── hooks/           # 自定义 Hook
│       └── types/           # 公共类型定义
├── ai-service/             # FastAPI AI 服务
│   └── app/
│       ├── api/             # inspector / assistant / knowledge / health
│       ├── core/            # 配置 (pydantic-settings)
│       └── services/        # 文档解析工具
├── wiki/                   # 项目文档（本目录）
├── docker-compose.yml      # 完整服务编排
└── nginx.conf              # Nginx 反向代理配置
```
