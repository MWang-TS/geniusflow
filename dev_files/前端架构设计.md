# GeniusFlow 前端架构设计

版本：V1.0（MVP）
技术栈：React 18 + TypeScript + Ant Design 5 + React Flow 12 + Zustand

---

## 1. 项目结构

```
geniusflow-web/
├── public/
│   └── index.html
├── src/
│   ├── main.tsx                 # 入口
│   ├── App.tsx                  # 根组件（路由 + 布局）
│   ├── routes.tsx               # 路由配置
│   │
│   ├── api/                     # API 客户端
│   │   ├── client.ts            # axios 实例 + 拦截器
│   │   ├── auth.ts              # 认证相关 API
│   │   ├── process-definition.ts
│   │   ├── process-instance.ts
│   │   ├── node-instance.ts
│   │   ├── task.ts
│   │   ├── knowledge-base.ts
│   │   ├── ai.ts
│   │   ├── progress.ts
│   │   ├── template.ts
│   │   └── upload.ts
│   │
│   ├── stores/                  # 全局状态（Zustand）
│   │   ├── auth.store.ts
│   │   ├── process-design.store.ts
│   │   ├── process-runtime.store.ts
│   │   ├── task.store.ts
│   │   ├── notification.store.ts
│   │   └── websocket.store.ts
│   │
│   ├── components/              # 公共组件
│   │   ├── common/
│   │   │   ├── PageHeader.tsx
│   │   │   ├── DataTable.tsx
│   │   │   ├── FileUploader.tsx
│   │   │   ├── RichTextEditor.tsx
│   │   │   ├── AiPanel.tsx
│   │   │   └── GanttChart.tsx
│   │   ├── layout/
│   │   │   ├── MainLayout.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── TopBar.tsx
│   │   │   └── Breadcrumb.tsx
│   │   └── process/
│   │       ├── ProcessCanvas.tsx
│   │       ├── NodePropertyPanel.tsx
│   │       ├── NodeLibrary.tsx
│   │       ├── ProcessMiniMap.tsx
│   │       └── NodeStatusBadge.tsx
│   │
│   ├── pages/                   # 页面组件
│   │   ├── login/
│   │   │   └── LoginPage.tsx
│   │   ├── process-design/
│   │   │   ├── ProcessListPage.tsx
│   │   │   ├── ProcessEditorPage.tsx
│   │   │   └── ProcessVersionPage.tsx
│   │   ├── employee-workspace/
│   │   │   ├── MyTasksPage.tsx
│   │   │   └── NodeExecutionPage.tsx
│   │   ├── manager-workspace/
│   │   │   ├── ApprovalCenterPage.tsx
│   │   │   ├── ApprovalDetailPage.tsx
│   │   │   └── ProgressMonitorPage.tsx
│   │   ├── knowledge-base/
│   │   │   ├── KnowledgeBaseListPage.tsx
│   │   │   └── KnowledgeBaseDetailPage.tsx
│   │   ├── template/
│   │   │   └── TemplateMarketPage.tsx
│   │   └── admin/
│   │       ├── UserManagementPage.tsx
│   │       └── RoleManagementPage.tsx
│   │
│   ├── hooks/                   # 自定义 Hooks
│   │   ├── useAuth.ts
│   │   ├── useProcessDefinition.ts
│   │   ├── useProcessInstance.ts
│   │   ├── useNodeInstance.ts
│   │   ├── useTasks.ts
│   │   ├── useWebSocket.ts
│   │   └── usePermission.ts
│   │
│   ├── types/                   # TypeScript 类型
│   │   ├── auth.ts
│   │   ├── process.ts
│   │   ├── node.ts
│   │   ├── task.ts
│   │   ├── ai.ts
│   │   └── common.ts
│   │
│   ├── utils/                   # 工具函数
│   │   ├── date.ts
│   │   ├── file.ts
│   │   ├── validator.ts
│   │   └── permission.ts
│   │
│   └── styles/
│       ├── global.css
│       ├── variables.less
│       └── process-editor.css
│
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

---

## 2. 路由设计

```typescript
// routes.tsx
import { createBrowserRouter } from 'react-router-dom';

export const router = createBrowserRouter([
  {
    path: '/login',
    element: <LoginPage />
  },
  {
    path: '/',
    element: <MainLayout />,
    children: [
      // 流程设计（designer, admin）
      {
        path: 'processes',
        element: <ProcessListPage />,
        roles: ['designer', 'admin']
      },
      {
        path: 'processes/:id/edit',
        element: <ProcessEditorPage />,
        roles: ['designer', 'admin']
      },
      
      // 员工工作台（employee, manager, admin）
      {
        path: 'my-tasks',
        element: <MyTasksPage />,
        roles: ['employee', 'manager', 'admin']
      },
      {
        path: 'nodes/:nodeInstanceId/execute',
        element: <NodeExecutionPage />,
        roles: ['employee', 'manager', 'admin']
      },
      
      // 管理者工作台（manager, admin）
      {
        path: 'approvals',
        element: <ApprovalCenterPage />,
        roles: ['manager', 'admin']
      },
      {
        path: 'approvals/:taskId',
        element: <ApprovalDetailPage />,
        roles: ['manager', 'admin']
      },
      {
        path: 'progress',
        element: <ProgressMonitorPage />,
        roles: ['manager', 'admin']
      },
      {
        path: 'progress/:instanceId',
        element: <ProgressDetailPage />,
        roles: ['manager', 'admin']
      },
      
      // 知识库（admin）
      {
        path: 'knowledge-bases',
        element: <KnowledgeBaseListPage />,
        roles: ['admin']
      },
      {
        path: 'knowledge-bases/:id',
        element: <KnowledgeBaseDetailPage />,
        roles: ['admin']
      },
      
      // 模板市场（all）
      {
        path: 'templates',
        element: <TemplateMarketPage />,
        roles: ['designer', 'employee', 'manager', 'admin']
      },
      
      // 系统管理（admin）
      {
        path: 'admin/users',
        element: <UserManagementPage />,
        roles: ['admin']
      },
      {
        path: 'admin/roles',
        element: <RoleManagementPage />,
        roles: ['admin']
      }
    ]
  }
]);
```

---

## 3. 状态管理（Zustand）

### 3.1 认证状态

```typescript
// stores/auth.store.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;
  login: (credentials: LoginCredentials) => Promise<void>;
  logout: () => void;
  refresh: () => Promise<void>;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      user: null,
      accessToken: null,
      refreshToken: null,
      isAuthenticated: false,
      
      login: async (credentials) => {
        const { data } = await authApi.login(credentials);
        set({
          user: data.user,
          accessToken: data.accessToken,
          refreshToken: data.refreshToken,
          isAuthenticated: true
        });
      },
      
      logout: () => {
        set({
          user: null,
          accessToken: null,
          refreshToken: null,
          isAuthenticated: false
        });
      },
      
      refresh: async () => {
        const { refreshToken } = get();
        if (!refreshToken) return;
        const { data } = await authApi.refresh(refreshToken);
        set({ accessToken: data.accessToken });
      }
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({
        accessToken: state.accessToken,
        refreshToken: state.refreshToken
      })
    }
  )
);
```

### 3.2 流程设计状态

```typescript
// stores/process-design.store.ts
interface ProcessDesignState {
  // 当前编辑的流程定义
  currentProcess: ProcessDefinition | null;
  
  // React Flow 节点和边
  nodes: Node[];
  edges: Edge[];
  
  // 选中的节点
  selectedNodeId: string | null;
  
  // 操作
  setNodes: (nodes: Node[]) => void;
  setEdges: (edges: Edge[]) => void;
  addNode: (type: string, position: XYPosition) => void;
  removeNode: (id: string) => void;
  selectNode: (id: string | null) => void;
  updateNodeData: (id: string, data: Partial<NodeData>) => void;
  
  // 保存/发布
  saveDraft: () => Promise<void>;
  publish: () => Promise<void>;
  
  // 校验
  validate: () => ValidationResult;
}

export const useProcessDesignStore = create<ProcessDesignState>((set, get) => ({
  currentProcess: null,
  nodes: [],
  edges: [],
  selectedNodeId: null,
  
  addNode: (type, position) => {
    const newNode: Node = {
      id: `node-${Date.now()}`,
      type,
      position,
      data: { label: '新节点', ...getDefaultNodeData(type) }
    };
    set({ nodes: [...get().nodes, newNode] });
  },
  
  validate: () => {
    const { nodes, edges } = get();
    const errors: string[] = [];
    
    // 必须有开始和结束节点
    const hasStart = nodes.some(n => n.type === 'start');
    const hasEnd = nodes.some(n => n.type === 'end');
    if (!hasStart) errors.push('缺少开始节点');
    if (!hasEnd) errors.push('缺少结束节点');
    
    // 不能有孤立节点
    const connectedNodeIds = new Set(edges.flatMap(e => [e.source, e.target]));
    nodes.forEach(n => {
      if (n.type !== 'start' && !connectedNodeIds.has(n.id)) {
        errors.push(`节点 "${n.data.label}" 未连接`);
      }
    });
    
    return { valid: errors.length === 0, errors };
  },
  
  // ...
}));
```

### 3.3 WebSocket 状态

```typescript
// stores/websocket.store.ts
interface WebSocketState {
  socket: WebSocket | null;
  isConnected: boolean;
  connect: () => void;
  disconnect: () => void;
  send: (message: WsMessage) => void;
  onMessage: (handler: (msg: WsMessage) => void) => void;
}

export const useWebSocketStore = create<WebSocketState>((set, get) => ({
  socket: null,
  isConnected: false,
  
  connect: () => {
    const token = useAuthStore.getState().accessToken;
    const socket = new WebSocket(`ws://api.example.com/ws?token=${token}`);
    
    socket.onopen = () => set({ isConnected: true });
    socket.onclose = () => set({ isConnected: false });
    socket.onmessage = (event) => {
      const msg = JSON.parse(event.data);
      // 分发到各 store
      if (msg.type === 'ai_inspection_result') {
        useProcessRuntimeStore.getState().handleAiResult(msg.payload);
      }
      if (msg.type === 'notification') {
        useNotificationStore.getState().addNotification(msg.payload);
      }
    };
    
    set({ socket });
  },
  
  disconnect: () => {
    get().socket?.close();
    set({ socket: null, isConnected: false });
  },
  
  send: (message) => {
    get().socket?.send(JSON.stringify(message));
  }
}));
```

---

## 4. 核心组件设计

### 4.1 流程设计器画布

```typescript
// components/process/ProcessCanvas.tsx
import ReactFlow, {
  Background,
  Controls,
  MiniMap,
  useNodesState,
  useEdgesState,
  addEdge
} from 'reactflow';
import 'reactflow/dist/style.css';

const nodeTypes = {
  start: StartNode,
  task: TaskNode,
  end: EndNode
};

export const ProcessCanvas: React.FC = () => {
  const { nodes, edges, setNodes, setEdges, selectNode } = useProcessDesignStore();
  
  const [rfNodes, setRfNodes, onNodesChange] = useNodesState(nodes);
  const [rfEdges, setRfEdges, onEdgesChange] = useEdgesState(edges);
  
  const onConnect = useCallback(
    (params: Connection) => setRfEdges((eds) => addEdge(params, eds)),
    [setRfEdges]
  );
  
  const onNodeClick = useCallback(
    (_: React.MouseEvent, node: Node) => selectNode(node.id),
    [selectNode]
  );
  
  return (
    <div style={{ width: '100%', height: '100%' }}>
      <ReactFlow
        nodes={rfNodes}
        edges={rfEdges}
        onNodesChange={onNodesChange}
        onEdgesChange={onEdgesChange}
        onConnect={onConnect}
        onNodeClick={onNodeClick}
        nodeTypes={nodeTypes}
        fitView
      >
        <Background />
        <Controls />
        <MiniMap />
      </ReactFlow>
    </div>
  );
};
```

### 4.2 节点属性面板

```typescript
// components/process/NodePropertyPanel.tsx
export const NodePropertyPanel: React.FC = () => {
  const { selectedNodeId, nodes, updateNodeData } = useProcessDesignStore();
  
  const node = nodes.find(n => n.id === selectedNodeId);
  if (!node) return <Empty description="请选择节点" />;
  
  return (
    <Tabs defaultActiveKey="basic">
      <TabPane tab="基本属性" key="basic">
        <Form layout="vertical">
          <Form.Item label="节点名称">
            <Input 
              value={node.data.label}
              onChange={e => updateNodeData(node.id, { label: e.target.value })}
            />
          </Form.Item>
          <Form.Item label="计划工期（天）">
            <InputNumber 
              value={node.data.plannedDuration}
              onChange={v => updateNodeData(node.id, { plannedDuration: v })}
            />
          </Form.Item>
          <Form.Item label="是否里程碑">
            <Switch 
              checked={node.data.isMilestone}
              onChange={v => updateNodeData(node.id, { isMilestone: v })}
            />
          </Form.Item>
        </Form>
      </TabPane>
      
      <TabPane tab="输入配置" key="input">
        <InputConfigForm 
          data={node.data.inputSpec}
          onChange={data => updateNodeData(node.id, { inputSpec: data })}
        />
      </TabPane>
      
      <TabPane tab="行动配置" key="action">
        <ActionConfigForm 
          data={node.data.actionSpec}
          onChange={data => updateNodeData(node.id, { actionSpec: data })}
        />
      </TabPane>
      
      <TabPane tab="输出配置" key="output">
        <OutputConfigForm 
          data={node.data.outputSpec}
          onChange={data => updateNodeData(node.id, { outputSpec: data })}
        />
      </TabPane>
      
      <TabPane tab="AI 配置" key="ai">
        <AIConfigForm 
          data={node.data.aiConfig}
          onChange={data => updateNodeData(node.id, { aiConfig: data })}
        />
      </TabPane>
    </Tabs>
  );
};
```

### 4.3 员工执行界面

```typescript
// pages/employee-workspace/NodeExecutionPage.tsx
export const NodeExecutionPage: React.FC = () => {
  const { nodeInstanceId } = useParams();
  const { data: nodeInstance, isLoading } = useNodeInstance(nodeInstanceId);
  const submitMutation = useSubmitNode();
  
  const [formData, setFormData] = useState({});
  const [percentComplete, setPercentComplete] = useState(0);
  const [aiInspecting, setAiInspecting] = useState(false);
  
  const handleSubmit = async () => {
    setAiInspecting(true);
    try {
      const result = await submitMutation.mutateAsync({
        nodeInstanceId,
        inputData: formData.input,
        outputData: formData.output,
        percentComplete
      });
      
      if (result.aiReport?.passed === false) {
        // 督导拦截，展示问题
        Modal.error({
          title: 'AI 督导未通过',
          content: <AiInspectionResult issues={result.aiReport.issues} />
        });
      } else {
        message.success('提交成功，等待审批');
        navigate('/my-tasks');
      }
    } finally {
      setAiInspecting(false);
    }
  };
  
  return (
    <div className="node-execution-page">
      <Row gutter={24}>
        {/* 左侧：三阶段表单 */}
        <Col span={16}>
          <Card title={nodeInstance?.nodeName}>
            <Collapse defaultActiveKey={['input', 'action', 'output']}>
              <Panel header="输入" key="input">
                <DynamicForm 
                  schema={nodeInstance?.inputSpec.dataSchema}
                  value={formData.input}
                  onChange={v => setFormData(prev => ({ ...prev, input: v }))}
                />
              </Panel>
              <Panel header="行动" key="action">
                <div dangerouslySetInnerHTML={{ 
                  __html: nodeInstance?.actionSpec.instructions 
                }} />
              </Panel>
              <Panel header="输出" key="output">
                <DynamicForm 
                  schema={nodeInstance?.outputSpec.deliverables}
                  value={formData.output}
                  onChange={v => setFormData(prev => ({ ...prev, output: v }))}
                />
              </Panel>
            </Collapse>
            
            <div className="progress-section">
              <span>完成进度：</span>
              <Slider 
                value={percentComplete}
                onChange={setPercentComplete}
              />
              <span>{percentComplete}%</span>
            </div>
            
            <Space>
              <Button onClick={handleSave}>保存草稿</Button>
              <Button 
                type="primary" 
                onClick={handleSubmit}
                loading={aiInspecting}
              >
                {aiInspecting ? 'AI 校验中...' : '提交'}
              </Button>
            </Space>
          </Card>
        </Col>
        
        {/* 右侧：AI 助手面板 */}
        <Col span={8}>
          <AiPanel 
            nodeInstanceId={nodeInstanceId}
            aiConfig={nodeInstance?.aiConfig}
          />
        </Col>
      </Row>
    </div>
  );
};
```

### 4.4 AI 助手面板

```typescript
// components/common/AiPanel.tsx
export const AiPanel: React.FC<AiPanelProps> = ({ nodeInstanceId, aiConfig }) => {
  const { data: aiReport } = useAiReport(nodeInstanceId);
  const [activeTab, setActiveTab] = useState('inspector');
  
  return (
    <Card title="AI 助手" className="ai-panel">
      <Tabs activeKey={activeTab} onChange={setActiveTab}>
        {aiConfig?.inspector?.enabled && (
          <TabPane tab="督导" key="inspector">
            {aiReport?.inspectorReport ? (
              <div>
                <Tag color={aiReport.inspectorReport.passed ? 'green' : 'red'}>
                  {aiReport.inspectorReport.passed ? '通过' : '未通过'}
                </Tag>
                <Progress 
                  percent={aiReport.inspectorReport.score}
                  status={aiReport.inspectorReport.passed ? 'success' : 'exception'}
                />
                <List
                  dataSource={aiReport.inspectorReport.issues}
                  renderItem={issue => (
                    <List.Item>
                      <Tag color={issue.type === 'error' ? 'red' : 'orange'}>
                        {issue.type === 'error' ? '错误' : '警告'}
                      </Tag>
                      <Text>{issue.message}</Text>
                      <Text type="secondary">{issue.suggestion}</Text>
                    </List.Item>
                  )}
                />
              </div>
            ) : (
              <Empty description="尚未提交，暂无督导报告" />
            )}
          </TabPane>
        )}
        
        {aiConfig?.assistant?.enabled && (
          <TabPane tab="助理" key="assistant">
            {aiReport?.assistantReport ? (
              <div>
                <Tag color={aiReport.assistantReport.recommendation === 'approve' ? 'green' : 'red'}>
                  建议{aiReport.assistantReport.recommendation === 'approve' ? '通过' : '驳回'}
                </Tag>
                <Text>{aiReport.assistantReport.summary}</Text>
              </div>
            ) : (
              <Empty description="审批时生成助理报告" />
            )}
          </TabPane>
        )}
      </Tabs>
    </Card>
  );
};
```

---

## 5. 权限控制

### 5.1 路由守卫

```typescript
// components/common/RouteGuard.tsx
export const RouteGuard: React.FC<{ roles: string[] }> = ({ roles, children }) => {
  const { user, isAuthenticated } = useAuthStore();
  
  if (!isAuthenticated) {
    return <Navigate to="/login" />;
  }
  
  const hasPermission = roles.some(role => user?.roles.includes(role));
  if (!hasPermission) {
    return <Result status="403" title="无权访问" />;
  }
  
  return <>{children}</>;
};
```

### 5.2 按钮级权限

```typescript
// hooks/usePermission.ts
export const usePermission = () => {
  const { user } = useAuthStore();
  
  const hasRole = (role: string) => user?.roles.includes(role) ?? false;
  const hasAnyRole = (roles: string[]) => roles.some(hasRole);
  
  return { hasRole, hasAnyRole };
};

// 使用
const { hasRole } = usePermission();
{hasRole('manager') && <Button>审批</Button>}
```

---

## 6. API 客户端

```typescript
// api/client.ts
import axios from 'axios';
import { useAuthStore } from '@/stores/auth.store';

const client = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
  timeout: 30000
});

// 请求拦截器：添加 Token
client.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：处理错误 + Token 刷新
client.interceptors.response.use(
  (response) => response.data,
  async (error) => {
    const originalRequest = error.config;
    
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;
      await useAuthStore.getState().refresh();
      return client(originalRequest);
    }
    
    // 统一错误处理
    const message = error.response?.data?.message || '请求失败';
    notification.error({ message });
    return Promise.reject(error);
  }
);

export default client;
```

---

## 7. 性能优化

| 优化点 | 方案 |
|--------|------|
| 流程定义缓存 | React Query 缓存， staleTime: 5min |
| 大列表虚拟滚动 | Ant Design Table 虚拟滚动 |
| React Flow 性能 | 节点数 > 50 时启用 `onlyRenderVisibleElements` |
| 图片/文件懒加载 | 使用 Intersection Observer |
| 代码分割 | 按路由懒加载页面组件 |
| WebSocket 重连 | 指数退避，最大重试 5 次 |

---

## 8. 响应式适配

```typescript
// 断点定义
const breakpoints = {
  xs: '480px',   // 手机
  sm: '768px',   // 平板
  md: '1024px',  // 小桌面
  lg: '1366px',  // 标准桌面（MVP 最低支持）
  xl: '1920px'   // 大桌面
};

// 布局响应式
<Row>
  <Col xs={24} lg={16}> {/* 左侧表单 */} </Col>
  <Col xs={24} lg={8}>  {/* 右侧 AI 面板 */} </Col>
</Row>

// MVP 最低分辨率 1366×768
// 手机端：V1.2 实现，MVP 仅保证审批列表基本可用
```
